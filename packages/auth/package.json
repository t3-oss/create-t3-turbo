{
  "name": "@gmacko/auth",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./middleware": "./src/middleware.ts",
    "./client": "./src/client.ts",
    "./env": "./env.ts"
  },
  "license": "MIT",
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "format": "biome check .",
    "lint": "oxlint .",
    "generate": "dotenv -e ../../.env -- pnpx @better-auth/cli generate --config script/auth-cli.ts --output ../db/src/auth-schema.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "verify:flows": "dotenv -e ../../.env -- tsx script/verify-flows.ts"
  },
  "dependencies": {
    "@better-auth/expo": "catalog:",
    "@better-auth/sso": "catalog:",
    "@gmacko/db": "workspace:*",
    "@gmacko/logging": "workspace:*",
    "@t3-oss/env-core": "^0.13.10",
    "better-auth": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@better-auth/cli": "catalog:",
    "@biomejs/biome": "catalog:",
    "@gmacko/config": "workspace:*",
    "@gmacko/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "dotenv-cli": "^11.0.0",
    "oxlint": "catalog:",
    "tsx": "^4.21.0",
    "typescript": "catalog:",
    "vitest": "^4.1.0"
  }
}
