{
  "name": "@gmacko/db",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    },
    "./client": {
      "types": "./dist/client.d.ts",
      "default": "./src/client.ts"
    },
    "./client-local": {
      "types": "./dist/client-local.d.ts",
      "default": "./src/client-local.ts"
    },
    "./schema": {
      "types": "./dist/schema.d.ts",
      "default": "./src/schema.ts"
    }
  },
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "tsc",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "lint": "eslint",
    "generate": "pnpm with-env drizzle-kit generate",
    "migrate": "pnpm with-env drizzle-kit migrate",
    "push": "pnpm with-env drizzle-kit push",
    "push:local": "DATABASE_DRIVER=pg pnpm with-env drizzle-kit push",
    "check": "pnpm with-env drizzle-kit check",
    "seed": "pnpm with-env tsx src/seed.ts",
    "seed:local": "DATABASE_DRIVER=pg pnpm with-env tsx src/seed.ts",
    "studio": "pnpm with-env drizzle-kit studio",
    "studio:local": "pnpm with-env drizzle-kit studio --config drizzle.config.local.ts",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "drizzle-orm": "^0.44.7",
    "drizzle-zod": "^0.8.3",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@gmacko/eslint-config": "workspace:*",
    "@gmacko/prettier-config": "workspace:*",
    "@gmacko/tsconfig": "workspace:*",
    "drizzle-kit": "^0.31.5",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "tsx": "^4.19.0",
    "typescript": "catalog:"
  },
  "prettier": "@gmacko/prettier-config"
}
