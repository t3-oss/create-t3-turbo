{
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "^db-generate"],
      "outputs": [".next/**", ".expo/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "dependsOn": ["^db-generate"],
      "cache": false
    },
    "db-generate": {},
    "db-push": {},
    "clean": {},
    "//#clean": {}
  }
}
